/*
package com.karthik.mapper;

import com.karthik.bean.CourseBean;
import com.karthik.entity.Course;
import org.mapstruct.Mapper;

import java.util.List;

@Mapper(componentModel = "spring")
public interface CourseMapper {
    CourseBean toCourseDTO(Course course);
    List<CourseBean> toCourseDTOs(List<Course> courses);
    Course toCourse(CourseBean courseDTO);
}
*/
